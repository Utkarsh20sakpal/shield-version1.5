{
  "name": "shield-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=18.x"
  }
}